<% debugger %>
<% if logged_in? %>
    <script type="text/javascripts">
        window.currentUser = <%= render(
            "api/users/user.json.jbuilder", 
            user: current_user
        ).html_safe %>
    </script>
<% end %>
<main id="root">
    <h3>Fix the code!</h3>
</main>